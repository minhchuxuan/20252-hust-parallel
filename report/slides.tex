% Presentation slides for the IT4130E group project.
% Compile:  latexmk -pdf slides.tex   or   tectonic slides.tex
\documentclass[aspectratio=169]{beamer}
\usetheme{Madrid}
\usecolortheme{seahorse}
\setbeamertemplate{navigation symbols}{}

\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tikz}

\graphicspath{{figures/}}

\title[Parallel Barnes--Hut $N$-body]{Distributed-Memory Parallelisation of a
Barnes--Hut $N$-Body Simulation}
\subtitle{IT4130E Parallel and Distributed Programming}
\author[Group]{Chu Xuan Minh \and Nguyen Binh Anh \and Doan Phuong Khang \and Nguyen Tien Phat}
\institute{IT4130E Parallel and Distributed Programming}
\date{\today}

\begin{document}

\begin{frame}\titlepage\end{frame}

\begin{frame}{Outline}\tableofcontents\end{frame}

% ------------------------------------------------------------------
\section{Problem}
\begin{frame}{The $N$-body problem}
\begin{columns}
\begin{column}{0.58\textwidth}
\begin{itemize}
  \item $N$ masses attract each other by Newtonian gravity; advance their
        motion through time.
  \item Force on a body sums contributions of all others:
        \[ \vec a_i = G\sum_{j\neq i} m_j\,
           \frac{\vec r_j-\vec r_i}{(\lVert\vec r_j-\vec r_i\rVert^2+\varepsilon^2)^{3/2}} \]
  \item Direct evaluation costs $\mathcal{O}(N^2)$ per step --- too slow.
  \item \textbf{Goal:} a correct sequential method, a distributed-memory (MPI)
        parallel version, and a hybrid MPI\,+\,OpenMP version, with a measured
        performance study.
\end{itemize}
\end{column}
\begin{column}{0.42\textwidth}
\includegraphics[width=\linewidth]{sim.png}
\end{column}
\end{columns}
\end{frame}

\begin{frame}{Barnes--Hut: $\mathcal{O}(N\log N)$}
\begin{columns}
\begin{column}{0.6\textwidth}
\begin{itemize}
  \item Recursively divide the plane into a \textbf{quadtree}; each leaf holds
        $\le 1$ body.
  \item Each node stores the total mass and centre of mass below it.
  \item Force on a body: traverse from the root. If a node is far enough
        ($s/d<\theta$), treat the whole subtree as one mass; else descend.
  \item $\theta=0.5$: about $1\%$ force error, $\mathcal{O}(N\log N)$ work.
  \item The tree makes the work \textbf{irregular} --- the key parallel
        challenge.
\end{itemize}
\end{column}
\begin{column}{0.4\textwidth}
\centering
\footnotesize
$s/d < \theta \Rightarrow$ approximate\\[4pt]
\begin{tikzpicture}[scale=0.9]
  \draw (0,0) rectangle (3,3);
  \draw (1.5,0)--(1.5,3); \draw (0,1.5)--(3,1.5);
  \draw (1.5,1.5) rectangle (2.25,2.25);
  \filldraw (1.9,1.9) circle (1pt);
  \filldraw (0.5,0.5) circle (1pt); \filldraw (0.8,2.4) circle (1pt);
  \filldraw (2.6,0.4) circle (1pt);
\end{tikzpicture}
\end{column}
\end{columns}
\end{frame}

% ------------------------------------------------------------------
\section{Parallelisation}
\begin{frame}{Design choices (the rubric questions)}
\begin{itemize}
  \item \textbf{Level of parallelism:} \emph{data} --- same operation, different
        bodies.
  \item \textbf{Decomposition:} \emph{data decomposition} (owner-computes per
        body) over a \emph{recursive} (replicated) tree build $\Rightarrow$
        hybrid, data-dominated.
  \item \textbf{Mapping:} \emph{1-D block} --- process $r$ owns a contiguous block
        of $\approx N/P$ bodies (not 2-D $\sqrt{P}\times\sqrt{P}$: the domain is a
        1-D list, not a grid).
  \item \textbf{Communication:} \emph{blocking, collective}; broadcast once at
        start, then one \emph{all-gather} of positions per step. Symmetric
        single-program multiple-data (SPMD),
        \emph{no} master--slave; no explicit ring/hypercube topology.
  \item \textbf{Load balance:} equal block \emph{size}; work per body is uneven
        (dense regions cost more).
\end{itemize}
\end{frame}

\begin{frame}{Parallel algorithm (process $r$ of $P$)}
\begin{algorithmic}[1]
\State \textbf{(once)} broadcast initial positions, velocities, masses
\State $lo,hi \gets$ block owned by process $r$ \Comment{1-D block}
\For{each time step}
  \State build the full quadtree locally \Comment{replicated}
  \State accumulate mass / centre of mass at each node
  \For{$i = lo \dots hi-1$} \Comment{OpenMP parallel-for in hybrid}
     \State $\vec a_i \gets$ traverse tree with $\theta$
  \EndFor
  \State $\vec v_i \mathrel{+}= \vec a_i\Delta t$; \; $\vec r_i \mathrel{+}= \vec v_i\Delta t$
  \State \textbf{all-gather} updated positions
\EndFor
\end{algorithmic}
\vspace{4pt}
\footnotesize Force traversal communicates nothing (tree is complete on every
process). Cost: build $\mathcal{O}(N)$ replicated $+$ force
$\mathcal{O}((N/P)\log N)$ $+$ comm $\mathcal{O}(N)$.
\end{frame}

% ------------------------------------------------------------------
\section{Results}
\begin{frame}{The cluster}
\begin{columns}
\begin{column}{0.54\textwidth}
\begin{itemize}
  \item \textbf{4 laptops} on a \textbf{phone Wi-Fi hotspot}; one Ubuntu VM each
        (bridged adapter).
  \item \textbf{2 cores per VM $\Rightarrow$ 8 cores total}, one MPI rank per core.
  \item Open MPI 4.1.2, Ubuntu 22.04, all nodes equal.
  \item Round-trip latency $\approx$ 4--5\,ms --- \textbf{this number decides
        everything that follows.}
\end{itemize}
\vspace{4pt}
\footnotesize Honest caveat: the hotspot was congested --- we report short runs,
min-of-repeats, and trust per-step costs rather than absolute wall-clocks.
\end{column}
\begin{column}{0.46\textwidth}
\centering\small
\begin{tabular}{@{}lcc@{}}
\toprule
Node & Cores & MPI \\
\midrule
node1 & 2 & 4.1.2 \\
node2 & 2 & 4.1.2 \\
node3 & 2 & 4.1.2 \\
node4 & 2 & 4.1.2 \\
\midrule
\textbf{total} & \textbf{8} & \\
\bottomrule
\end{tabular}
\end{column}
\end{columns}
\end{frame}

\begin{frame}{Correctness}
\begin{itemize}
  \item MPI output matches the sequential reference \textbf{bit for bit}:
        \begin{itemize}\footnotesize
          \item single rank: max difference $= 0$;
          \item 8 ranks across all 4 machines: max difference $= 0$ (float tolerance).
        \end{itemize}
  \item Each body uses identical arithmetic; the all-gather moves data without
        changing it $\Rightarrow$ the network adds no error.
  \item Total energy conserved to within \textbf{0.6\%} over 200 steps $\Rightarrow$
        physically stable.
\end{itemize}
\end{frame}

\begin{frame}{Headline result: communication dominates}
\begin{columns}
\begin{column}{0.5\textwidth}
\includegraphics[width=\linewidth]{comm_share.png}
\end{column}
\begin{column}{0.5\textwidth}
\begin{itemize}
  \item \textbf{2 blocking all-gathers per step} on a 4--5\,ms link.
  \item Communication is \textbf{97--99\%} of wall-clock at \emph{every} $N$.
  \item Latency $\times$ steps, not data volume, is the cost.
  \item So we always plot \textbf{communication and computation separately.}
\end{itemize}
\end{column}
\end{columns}
\end{frame}

\begin{frame}{Running time vs problem size (Experiment A)}
\begin{columns}
\begin{column}{0.6\textwidth}
\includegraphics[width=\linewidth]{scaling_n.png}
\end{column}
\begin{column}{0.4\textwidth}
\begin{itemize}
  \item 8 cores, vary $N$.
  \item \textbf{Left:} wall-clock $\approx$ communication (they overlap).
  \item \textbf{Right:} compute alone --- the true $\mathcal{O}(N\log N)$ cost,
        $100\times$ smaller.
  \item Operating size $N=20{,}000$: $\approx$1\,s/step $\Rightarrow$ 2--3\,min
        $\approx$ 155--185 steps.
\end{itemize}
\end{column}
\end{columns}
\end{frame}

\begin{frame}{Granularity \& load balance (Experiment B)}
\begin{columns}
\begin{column}{0.58\textwidth}
\includegraphics[width=\linewidth]{granularity.png}
\end{column}
\begin{column}{0.42\textwidth}
\begin{itemize}
  \item \textbf{Left:} comm (blue) dwarfs compute (green) on every rank.
  \item \textbf{Right:} compute alone --- balanced (12--23\,ms across ranks).
  \item Idle-time imbalance $<25\%$: compute is balanced\ldots
  \item \ldots but compute balance barely matters when comm is 99\%.
\end{itemize}
\end{column}
\end{columns}
\end{frame}

\begin{frame}{Speed-up at $2N=40{,}000$ (Experiment C)}
\includegraphics[width=\linewidth]{speedup.png}
\vspace{2pt}
\footnotesize
\textbf{Left:} end-to-end wall-clock \emph{rises} once the network is used (np=4 is a
congestion outlier). \quad\textbf{Middle:} compute alone keeps shrinking.
\quad\textbf{Right:} compute-only speed-up $\approx 5\times$ on 8 cores (near ideal);
end-to-end stays $<1$ (a slowdown). \emph{The work parallelises; the network does not.}
\end{frame}

% ------------------------------------------------------------------
\section{Conclusion}
\begin{frame}{Conclusion}
\begin{itemize}
  \item Data-parallel Barnes--Hut on a real 4-machine cluster; output
        \textbf{bit-identical} to serial, energy conserved.
  \item \textbf{Communication-bound:} 2 blocking all-gathers/step on a 4--5\,ms
        Wi-Fi link $\Rightarrow$ comm is 97--99\% of run time, end-to-end speed-up
        $<1$.
  \item Separating the costs shows the \textbf{computation scales $\approx5\times$
        on 8 cores} --- the parallelisation is sound; the network is the limit.
  \item \textbf{Fix = communicate less often:} exchange every few steps, overlap
        with non-blocking comm, or distribute the tree (boundary data only).
\end{itemize}
\vspace{6pt}
\centering\large Thank you --- questions?
\end{frame}

\end{document}
